# ForesightGraph

Minimal Python scaffold for the ForesightGraph project.

Quick checks:

- Run tests: `python -m pytest tests`

Notes:
- This is an initial scaffold. No packages are installed by this change.