[project]
name = "foresightgraph"
version = "0.0.0"
description = "Minimal scaffold for ForesightGraph"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["src"]
